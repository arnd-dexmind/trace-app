@AGENTS.md

## Claude Code

Use the shared repository instructions from `AGENTS.md`. Claude-specific local notes may exist under `.claude/`, but that directory is gitignored and should not be treated as team-shared project guidance.

Keep general repository instructions in `AGENTS.md` so all coding agents can share them. Reserve `CLAUDE.md` for importing `AGENTS.md` and for Claude Code-specific instructions only.
